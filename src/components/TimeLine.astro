---
import Carrera from '../data/carrera.json'
---

<div class="studies-time-line">
    <div>
        {Carrera.map(({ date, title, description }) => (
            <div>
                <p class="date">{date}</p>
                <p class="text-gradient">{title}</p>
                <p class="description">{description}</p>
            </div>
        ))}
    </div>
</div>

<style>
    .studies-time-line {
        border-inline-style: solid;
        border-inline-color: var(--text-color);
        border-inline-width: 1px;
        border-right: none;
        padding: 0 2rem;
        margin: 2rem 5px;
    }
    .studies-time-line div {
        padding: 15px 0;
    }
    .studies-time-line .date {
        color: rgba(var(--accent));
        font-size: 0.9rem;
        padding: 0.5rem 0;
    }
    .date::before {
        content: "";
        width: 0.9rem;
        height: 0.9rem;
        background-color: rgb(var(--accent));
        border-radius: 50%;
        display: inline-block;
        top: 0.1rem;
        left: -1.5rem;
        position: relative;
        margin-left: -1rem;
    }
</style>